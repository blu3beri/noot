Noot

Node version manager.

allows
 - set (sets a downloaded node version)
 - add (downloads a new node version)
 - remove (removes a downloaded node version)
 - list (lists remote and local node versions) TODO
